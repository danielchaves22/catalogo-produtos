// frontend/pages/catalogos/novo.tsx
import React from 'react';
import CatalogoForm from './[id]';

// Componente simplificado que apenas renderiza o formulário de catálogo
export default function NovoCatalogoPage() {
  return <CatalogoForm />;
}